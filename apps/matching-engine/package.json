{
  "name": "@mock-kabu/matching-engine",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "start": "tsx src/main.ts",
    "build": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@mock-kabu/db": "workspace:*",
    "@mock-kabu/shared": "workspace:*",
    "dotenv": "^17.4.2",
    "ioredis": "^5.6.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.8.0",
    "vitest": "^3.1.0"
  }
}
