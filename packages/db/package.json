{
  "name": "@mock-kabu/db",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "prisma generate && tsc -p tsconfig.json",
    "generate": "prisma generate",
    "migrate:dev": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy",
    "seed": "tsx prisma/seed.ts",
    "check:consistency": "tsx scripts/check-consistency.ts",
    "recover:settlement": "tsx scripts/recover-unsettled-trades.ts"
  },
  "dependencies": {
    "@prisma/client": "^6.8.0",
    "@mock-kabu/shared": "workspace:*"
  },
  "devDependencies": {
    "bcryptjs": "^3.0.2",
    "prisma": "^6.8.0",
    "tsx": "^4.19.0",
    "typescript": "^5.8.0"
  }
}
